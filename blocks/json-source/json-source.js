/*
 * Json Source Block
 * Content (jsonData) is populated by the App Builder Universal Editor
 * extension's panel, not fetched here — this block only decorates
 * whatever's already been generated and saved.
 */

export default function decorate(block) {
  block.classList.add('json-source');
}
